{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://chirpstack-provisioning.schemas/tenant.json",
  "title": "ChirpStack Tenant",
  "description": "Schema for provisioning a ChirpStack tenant",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 100,
      "description": "Tenant name (must be unique)"
    },
    "description": {
      "type": "string",
      "maxLength": 500,
      "description": "Optional description of the tenant"
    },
    "can_have_gateways": {
      "type": "boolean",
      "description": "Whether this tenant can have gateways",
      "default": true
    },
    "max_device_count": {
      "type": "integer",
      "minimum": 0,
      "description": "Maximum number of devices allowed (0 = unlimited)",
      "default": 0
    },
    "max_gateway_count": {
      "type": "integer",
      "minimum": 0,
      "description": "Maximum number of gateways allowed (0 = unlimited)",
      "default": 0
    },
    "tags": {
      "type": "object",
      "description": "Tenant tags for organization and filtering",
      "additionalProperties": {
        "type": "string"
      }
    }
  },
  "required": ["name"]
}